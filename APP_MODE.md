# وضع التطبيق في AI DIGITAL SINAI

الإصدار الحالي يفتح على `/` كتجربة **App Mode/PWA** عربية باتجاه RTL، مع مسار `/app` مطابق، بينما تبقى الصفحة التعريفية محفوظة في `/landing`. الواجهة مصممة بعرض هاتف، وتنقل سفلي، وحالات فارغة صادقة، وخارطة تنفيذ احترافية تُقرأ من `/api/app-data` وتعرض لكل مرحلة حالتها وتفاصيل إعدادها.

## خارطة التنفيذ

تعرض الواجهة المراحل الأربع من المصدر الخادمي: بيانات الأعمال والصلاحيات، الدفع والتسوية، الجودة والمراقبة، ثم الإصدار Native. الحالات ليست مؤشرات نجاح تجريبية؛ `ready` يعني أن بوابات الكود متاحة، و`requires-setup` يعني أن إعداداً أو مزوداً خارجياً مطلوب، و`deferred` يعني أن المرحلة مؤجلة صراحة.

## الاتصال

يستدعي التطبيق `GET /api/health` عند التشغيل ويعرض ثلاث حالات واضحة: جارٍ التحقق، متصل، أو تعذر الاتصال. هذا endpoint يثبت صحة الخادم فقط ولا يدّعي اتصالاً ببيانات الأعمال. ربط البيانات الحقيقية يحتاج عقود tRPC أو REST من مشروع full-stack الفعلي.

## الدفع

يوفر الخادم `POST /api/payments/webhook` كحد آمن أولي. عند غياب `PAYMENT_WEBHOOK_SECRET` يعيد `503` ولا يسوي أي معاملة. عند وجود السر يتحقق من HMAC-SHA256 في ترويسة `x-payment-signature`؛ التوقيع غير الصحيح يعيد `403`، والتوقيع الصحيح يعيد `202 verified-pending` فقط. لا توجد تسوية مالية أو معالجة مزود داخل هذا الإصدار. أضيف اختبار وحدة يثبت قبول HMAC الصحيح ورفض payload المعدل أو التوقيع المشوه، ويعمل عبر `pnpm test`.

## التحويل إلى تطبيق Native

لإصدار APK أو IPA يجب نقل shell إلى مشروع Expo SDK 54، استعمال `ScreenContainer` و`FlatList` وExpo Router، ربط عميل tRPC بالمشروع full-stack، إضافة اختبارات E2E على Android/iOS، ثم إعداد package identifiers والتوقيع. لا ينبغي إنتاج APK اعتماداً على App Mode وحده. وضع التطبيق الحالي هو طبقة قابلة للتثبيت والمعاينة؛ الإصدار Native يحتاج تنفيذ Expo فعلياً، لا مجرد تغيير اسم المسار.
