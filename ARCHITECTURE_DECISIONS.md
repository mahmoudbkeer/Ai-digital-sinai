# قرارات معمارية — AI DIGITAL SINAI

## ADR-001: فصل App Mode عن طبقة التنفيذ

**القرار:** تبقى واجهة App Mode/PWA طبقة عرض وتنقل عربية RTL، بينما تمر كل عملية حساسة عبر API خادمي يتحقق من المدخلات والسياق والتوقيع وIdempotency قبل أي تنفيذ.

**السبب:** منع اعتماد المتصفح كمصدر ثقة، ودعم نقل الواجهة لاحقاً إلى Expo دون إعادة تعريف عقود الأعمال.

**الحالة:** منفذ جزئياً في `POST /api/commands/prepare`؛ ما زال التنفيذ الإنتاجي وقاعدة البيانات وسجل التدقيق الكامل مؤجلاً.

## ADR-002: liveness مقابل readiness

**القرار:** يستخدم `/api/health` لإثبات حياة العملية، ويستخدم `/api/readiness` لإظهار جاهزية الاعتماديات، مع حالة `503 degraded` عند غياب مزود حساس.

**الحالة:** منفذ.

## ADR-003: مسار Native لاحقاً

**القرار:** لا يُنشأ APK قبل اكتمال عقود backend والعزل متعدد المستأجرين واختبارات التدفقات الحرجة. بعد ذلك ينقل shell إلى Expo مع إبقاء عقود API مستقلة عن الواجهة.

**الحالة:** مؤجل ومعلن في خارطة التطبيق.
