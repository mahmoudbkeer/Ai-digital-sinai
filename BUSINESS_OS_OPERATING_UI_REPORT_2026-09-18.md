# تقرير تنفيذ واجهة التشغيل الفعلية لـ Business OS

**التاريخ:** 18 سبتمبر 2026  
**النطاق:** تحويل وحدات Business OS الخمس في قطاع التجارة والتجزئة من كتالوج وصفي إلى واجهة تشغيل متصلة ببيانات الخادم.

## الخلاصة التنفيذية

اكتمل تحويل الوحدات الخمس التالية إلى واجهة تشغيل فعلية: **المنتجات، المخزون، المبيعات والطلبات، العملاء، والموردون**. أصبحت كل وحدة تستخدم عميل API مركزيًا يرسل رمز Bearer ومعرّف المستأجر في كل طلب. تعرض الواجهة سجلات قاعدة البيانات الحقيقية داخل نطاق المستأجر الحالي، وتعرض حالات التحميل والخطأ ورفض الصلاحية. كما أضيفت عمليات إدارة أساسية من داخل كل وحدة مع إعادة تحميل البيانات بعد نجاح العملية.

لم تُجرَ أي تعديلات على Payment أو Checkout. بقيت تلك المسارات خارج نطاق التغيير.

## ما تم تنفيذه

### العميل المحمي

تم توسيع `client/src/lib/businessOsApi.ts` ليكون عقدًا موحدًا للوحدات الخمس. يحدد العقد مسار القراءة، مفتاح السجلات في الاستجابة، وبيانات عملية الكتابة لكل وحدة. يرسل العميل `Authorization: Bearer ...` و`x-tenant-id`، ويحوّل أخطاء HTTP إلى أخطاء قابلة للعرض في الواجهة.

يتم استخراج سياق النشاط والفرع من `/api/platform/me` بعد توفر جلسة المستخدم. هذا السياق ضروري لعمليات إنشاء المنتج والمورد وحركات المخزون، ويمنع إرسال عمليات كتابة قبل اكتمال سياق المصادقة.

### البيانات الفعلية

تقرأ الوحدات البيانات من المسارات التالية:

| الوحدة | مسار القراءة | مفتاح البيانات | عملية الإدارة الأساسية |
|---|---|---|---|
| المنتجات | `/api/platform/products` | `products` | إنشاء منتج عبر `POST` |
| المخزون | `/api/platform/inventory` | `stock` | تسجيل حركة عبر `/inventory/movements` |
| المبيعات والطلبات | `/api/platform/orders` | `orders` | تحديث حالة طلب عبر `/orders/:orderId/state` |
| العملاء | `/api/platform/customers` | `customers` | إنشاء عميل عبر `POST` |
| الموردون | `/api/platform/suppliers` | `suppliers` | إنشاء مورد عبر `POST` |

تعرض الواجهة حتى 20 سجلًا في الوحدة مع قيمة تعريفية وملخص للحقول الأساسية. إذا لم توجد سجلات، تظهر رسالة صريحة تؤكد أن المصدر خادم حقيقي ولا توجد بيانات تجريبية بديلة.

### عمليات الإدارة

عند اختيار عملية الوحدة، يظهر نموذج مرتبط بعقد API الخاص بها. بعد الإرسال، تعرض الواجهة حالة التنفيذ، وتعيد تحميل بيانات الوحدة من الخادم عند النجاح. عند رفض المصادقة أو الصلاحية، تُعرض رسالة منفصلة لحالة `401` أو `403`، ولا تُعتبر العملية ناجحة محليًا.

حالة تحديث الطلب تقبل حالات الخادم فقط. ويظل انتقال الحالة خاضعًا لقواعد الخادم الانتقالية وRBAC، وليس لقائمة محلية في المتصفح.

## Tenant Isolation وRBAC

لم تُضف أي قراءة مباشرة من المتصفح لقاعدة البيانات. جميع القراءات والكتابات تمر عبر API الخادم الحالي. يحتفظ الخادم بعناوين نطاق المستأجر، والتحقق من Bearer token، وفحوص النطاق والصلاحيات الموجودة مسبقًا.

تستخدم عمليات الخادم الحالية صلاحيات المجال، ومنها صلاحيات المنتج والمخزون والطلب وCRM والمورد. لذلك لا تمنح الواجهة صلاحية جديدة؛ هي فقط تستدعي عقودًا محمية وتتعامل مع الرفض الأمني كما هو.

## التحقق والتنفيذ

| الفحص | النتيجة |
|---|---|
| `pnpm check` | PASS |
| `pnpm exec vitest run server/businessOs.test.ts` | PASS — 7/7 اختبارات |
| اختبارات `commandAuth` و`commandEndpoint` و`commandPolicy` | PASS — 13/13 اختبارًا |
| `pnpm build` | PASS — Vite وesbuild اكتملَا بنجاح |
| `pnpm exec playwright test e2e/business-os-connectivity.spec.ts` | PASS — 1/1 اختبار |
| `git diff --check` | PASS |

اختبار Playwright أنشأ مستأجرًا وبيانات منتجات ومخزون وعميل ومورد وطلب، ثم فتح الوحدات الخمس وتحقق من أن كل وحدة استدعت مسار API الخاص بها وأظهرت البيانات الحقيقية داخل الواجهة.

## ملاحظة بيئية

فشل تشغيل `pnpm dev` عبر `scripts/dev.mjs` لأن السكربت يستدعي نسخة pnpm من مسار ثابت غير موجود في بيئة الاستنساخ: `/home/ubuntu/.local/share/pnpm/.tools/pnpm/10.4.1/bin/pnpm`. لم يكن هذا فشلًا في الكود المعدّل. تم تشغيل API وVite مباشرة باستخدام الأدوات المحلية، ثم نجح اختبار Playwright كاملًا.

هذه الملاحظة لا تغيّر نتيجة البناء أو الاختبارات، لكنها تستحق إصلاحًا بيئيًا مستقلًا إذا كان تشغيل `pnpm dev` مطلوبًا في بيئة جديدة.

## الملفات المعدلة

- `client/src/lib/businessOsApi.ts`: تعريف عقد القراءة والكتابة وعميل API المحمي للوحدات الخمس.
- `client/src/pages/MobileApp.tsx`: نماذج العمليات، تحميل سياق النشاط المصادق عليه، حالات التنفيذ، وإعادة تحميل البيانات.
- `BUSINESS_OS_OPERATING_UI_REPORT_2026-09-18.md`: هذا التقرير.

## حدود التغيير

لم يتم تعديل مسارات الدفع أو التسوية أو Checkout. ولم يتم تغيير مخطط قاعدة البيانات أو قواعد RBAC أو عزل المستأجر. الوحدات غير المرتبطة بالمسارات الخمسة الحالية ما زالت تعمل ككتالوج تشغيلي وصفي، وهذا التقرير يغطي وحدات التجارة والتجزئة الخمس التي لها API فعلية ومتكاملة في المستودع.
