# Mobile integration — AI DIGITAL SINAI

## قرار الدمج

تمت إضافة نسخة Expo الأصلية داخل `mobile/` على فرع `integration/mobile-expo` بدلاً من استبدال تطبيق الويب الموجود في الجذر. هذا القرار يحافظ على إعداد Vite/Express الحالي، ويمنع خلط Metro مع Vite أو خلط إعدادات React Native مع واجهة PWA.

## حماية البيانات

لا تُنفَّذ أي migration أو عملية حذف على قاعدة بيانات المستودع أثناء الدمج. مجلد `mobile/` يحمل عقوداً ومخطط Drizzle الخاصين بالنسخة الجوالة، لكن تشغيل migrations يظل منفصلاً ويتطلب `DATABASE_URL` مقدماً عبر مدير الأسرار. لا توجد قيم اتصال أو مفاتيح داخل المستودع، وتم استبعاد ملف إعدادات الجلسة المحلي الذي كان يحتوي أسراراً.

## ما تم نقله

نُقلت شاشات Expo، Expo Router، الهوية البصرية، الأيقونات، الحالة المحلية، عقود tRPC، خادم Node، طبقة Drizzle، اختبارات الصلاحيات، اختبارات logout، وثائق الأمن وAPI ومطابقة TXT. لم تُنقل `node_modules` أو `.git` أو `.expo` أو السجلات المؤقتة أو نواتج البناء.

## حدود البناء

يبقى بناء الويب الجذري كما هو. لتشغيل النسخة الجوالة استخدم `cd mobile && pnpm install && pnpm check && pnpm test && pnpm build`. لا تضف تبعيات الجوال إلى `package.json` الجذري ولا تستبدل `pnpm-lock.yaml` الجذري. قبل دمج الفرع في `main` يجب تشغيل فحوص الجذر وفحوص `mobile` كل على حدة.

## خطة الرجوع

إذا ظهر تعارض أو regression، يمكن حذف فرع `integration/mobile-expo` أو عكس commit الدمج دون لمس `main` أو قاعدة البيانات. لا تُطبّق migrations على الإنتاج قبل نسخة احتياطية واختبار استعادة ومراجعة SQL الناتج.

## الخطوات اللاحقة

يجب أولاً فتح Pull Request ومراجعة diff. بعد موافقة المراجعة، تُضاف بيئات التطوير والاختبار والإنتاج عبر مدير الأسرار، ثم تُنفذ اختبارات التكامل مع قاعدة البيانات الفعلية. لا يُبنى APK قبل اعتماد هذه الخطوات وإكمال مزودي الدفع والإشعارات واللوجستيات المطلوبة.
