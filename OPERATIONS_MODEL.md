# نموذج التشغيل القطاعي

يعتمد App Mode على قاموس تشغيل موحد في `client/src/lib/operationsCatalog.ts`. يتكون النموذج من قطاع، ثم وحدة داخل القطاع، ثم عملية، ثم حالة تشغيل. هذا يمنع تكرار الشاشات ويضمن أن كل تصنيف يملك مساراً داخلياً متسقاً.

| المستوى | المسؤولية | سلوك التطبيق |
|---|---|---|
| القطاع | تحديد المجال التجاري أو المجتمعي | يفتح قائمة الوحدات الخاصة به |
| الوحدة | تجميع وظائف متقاربة مثل الكتالوج والطلبات والرؤية | تفتح العمليات الخاصة بالوحدة |
| العملية | أمر قابل للفهم مثل إنشاء عرض أو قراءة الطلبات | تفتح مركز الأمر وتشرح ما يلزم قبل التنفيذ |
| الحالة | جاهز، يتطلب إعداداً، أو مؤجل | تمنع الادعاء بتنفيذ غير متاح وتوجه المستخدم للخطوة الصحيحة |

يغطي القاموس أربعة عشر قطاعاً: التجارة والتجزئة، الأغذية والمشروبات، الخدمات المنزلية، الحرف والصناعات، الزراعة والإنتاج، الصيد والبحر، النقل واللوجستيات، السياحة والضيافة، التعليم والتدريب، الصحة والعافية، العقارات، المقاولات والصيانة، الوظائف والمهارات، والمجتمع والجهات.

الضغط على «التشغيل» يفتح القطاعات. الضغط على قطاع يفتح وحداته، والضغط على وحدة يفتح عملياتها، والضغط على «فتح الأمر» يرسل `sectorId` و`moduleId` و`operationId` إلى `POST /api/commands/prepare` بعد تحقق الخادم من شكل المدخلات. يعيد الخادم حالة استقبال واشتراط المصادقة ومساحة عمل مصرح بها قبل التنفيذ. الأوامر المؤجلة تشرح سبب التأجيل، ولا ينفذ أي أمر حساس أو يوحي بنجاح بيانات غير متصلة.

## نقطة الدمج الخلفية

الواجهة جاهزة لاستبدال القاموس المحلي ببيانات tRPC أو API محمية على مستوى tenant. عند تفعيل الخادم الإنتاجي يجب أن يحمل كل كيان `tenantId` أو علاقة عضوية قابلة للتحقق، وأن تُنقل حالة العملية من العرض المحلي إلى سجل تدقيق خادمي قبل السماح بعمليات مالية أو لوجستية أو صحية.
