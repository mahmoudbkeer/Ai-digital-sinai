# تقرير المراجعة الأمنية — AI DIGITAL SINAI

## نطاق المراجعة

راجعت طبقة الخادم Express، مسارات الصحة والـwebhook والأوامر، سياسة التحقق الجديدة، طبقة tRPC وtenant access، مخطط Drizzle، واجهة App Mode/PWA، إعدادات البناء، ملفات البيئة، الاختبارات، وسجل الاعتماديات عبر `pnpm audit --prod`. هذا التقرير يصف ما تم فحصه فعلياً في هذه النسخة ولا يستبدل اختبار اختراق مستقل قبل الإنتاج.

## الضوابط المطبقة

| المجال | الضابط | الحالة |
|---|---|---|
| عزل المستأجرين | إجراءات tRPC الحساسة تمر عبر طبقة `tenantAccess` ولا تعتمد على معرّف العميل وحده | مطبق في المسارات الحالية |
| المصادقة | Manus OAuth والجلسة الخادمية عبر القالب full-stack | مطبق، ويتطلب إعداد بيئة OAuth في بيئة النقل |
| التفويض | أدوار ومساحات عمل وسجل تدقيق في طبقة البيانات | مطبق في العقود الحالية، ويحتاج اختباراً مستقلاً لكل دور عند إضافة إجراء جديد |
| الأوامر | تحقق من القطاع والوحدة والعملية وعلاقة المعرفات قبل قبول الأمر | أضيف في هذه المراجعة |
| الدفع | HMAC-SHA256، رفض التوقيع الخاطئ، وعدم التسوية تلقائياً | مطبق؛ التسوية ما زالت متوقفة حتى اعتماد مزود رسمي |
| حجم المدخلات | حد 128KB لـJSON و256KB لحمولة webhook | مطبق |
| الحد من الإساءة | حد burst داخل العملية لمسارات الأوامر والـwebhook | مطبق كخط دفاع أول؛ يلزم Redis أو بوابة API في الإنتاج متعدد النسخ |
| ترويسات HTTP | `nosniff` و`DENY` وReferrer Policy | مطبق؛ يوصى بإضافة CSP وHSTS من طبقة الاستضافة قبل النشر العام |
| الأسرار | لا توجد ملفات `.env*` داخل الحزمة؛ لا تُضمّن المفاتيح في المصدر | مطبق |
| البيانات الوهمية | لا توجد مراجعات أو تقييمات أو نجاحات مالية مختلقة | مطبق، وحالات الجاهزية صادقة |

## تحقق سياق الأوامر

يطلب `POST /api/commands/prepare` الآن `x-command-user` و`x-workspace-id` و`x-command-context-signature`. يُتحقق من التوقيع باستخدام HMAC-SHA256 عبر `COMMAND_CONTEXT_SECRET` أو `JWT_SECRET`، ويُرفض الغياب بـ401 والسياق غير الصالح بـ403. هذه طبقة تحقق خادمية أولية لسياق مصادق عليه من gateway أو جلسة لاحقة، ولا تُعد بديلاً عن ربط Manus OAuth وعضوية tenant بقاعدة البيانات قبل الإنتاج.

## نتائج الاعتماديات

أظهر `pnpm audit --prod` في الفحص الحالي 71 تنبيهاً (منها 16 عالية و47 متوسطة و8 منخفضة) في تبعيات مباشرة أو غير مباشرة، ومن بينها `mdast-util-to-hast` و`lodash-es` و`follow-redirects` و`uuid` و`mermaid` و`dompurify` و`lodash`. لم يتم تحديثها تلقائياً داخل هذه الحزمة لأن ذلك قد يغيّر شجرة الاعتماديات أو سلوك العرض دون اختبار توافق. يحفظ GitHub Actions تقرير الفحص كـartifact ولا يخفيه، بينما تبقى بوابة البناء والاختبارات حاجبة. قبل الإنتاج، نفّذ تحديثاً مقيداً مع مراجعة lockfile ثم شغّل `pnpm check`, `pnpm test`, `pnpm test:e2e`, و`pnpm build`.

## المخاطر المتبقية

> **مهم:** الحماية الحالية تمنع التنفيذ غير المصرح به في المسارات الموجودة، لكنها لا تعني أن مزود الدفع أو SMS أو Push أصبح متصلاً. لا يجب اعتبار `verified-pending` تسوية مالية.

الحد من المعدل داخل الذاكرة لا يتشارك الحالة بين النسخ ولا يستمر بعد إعادة التشغيل؛ لذلك يجب استبداله بمحدد موزع أو WAF قبل التوسع. كما يجب إضافة Content-Security-Policy وStrict-Transport-Security من طبقة الاستضافة بعد تأكيد أن جميع الموارد تعمل عبر HTTPS. ويجب إجراء اختبار اختراق خارجي، ومراجعة صلاحيات كل إجراء جديد، وفحص سجلات التدقيق مع إخفاء الأسرار والرموز الشخصية.

## بوابة قبول ما قبل الإنتاج

لا تُفعّل الدفع أو البيانات الحقيقية قبل تحقق العناصر التالية: اعتماد مزود رسمي وعقود توقيع موثقة، اختبارات idempotency وإعادة الإرسال، تحقق tenant isolation على قاعدة بيانات مملوءة ببيانات اختبار غير إنتاجية، اختبار صلاحيات لكل دور، CSP/HSTS، مراقبة مركزية، تدوير مفاتيح، نسخ احتياطي واستعادة مجرّبة، واختبار اختراق مستقل.

## الاختبارات

الاختبارات الجديدة في `server/commandPolicy.test.ts` تثبت قبول tuple معروف ورفض cross-sector والمعرفات العشوائية والقيم غير النصية. اختبارات `server/payment.test.ts` تثبت HMAC الصحيح ورفض الحمولة المعدلة والتوقيع المشوه.
