# دليل النقل والتكملة — AI DIGITAL SINAI

## محتويات الحزمة

هذه الحزمة هي مصدر المشروع القابل للتعديل: واجهة React/TypeScript عربية RTL، خادم Express، عقود tRPC، مخطط Drizzle، اختبارات Vitest وPlaywright، ملفات PWA، كتالوج القطاعات، وثائق التشغيل والأمان. لا تحتوي على `node_modules` أو `dist` أو `.env` أو مفاتيح سرية.

## المتطلبات

استخدم Node.js 22 أو إصداراً متوافقاً مع `package.json`، ومدير الحزم pnpm، وقاعدة MySQL/TiDB عند تشغيل طبقة البيانات. يلزم أيضاً إعداد Manus OAuth والمتغيرات الخادمية الموجودة في وثائق القالب. لا تضع القيم السرية داخل Git أو داخل ملفات الواجهة التي تبدأ بـ`VITE_` إلا إذا كانت عامة ومقصودة لذلك.

## تشغيل محلي

```bash
pnpm install
pnpm check
pnpm test
pnpm test:e2e
pnpm build
pnpm dev
```

يستخدم الخادم `PORT` من البيئة ولا يفترض منفذاً ثابتاً. افتح الجذر `/` لتجربة App Mode، ويمكن استخدام `/app` للمسار المطابق. نقطة `/api/app-data` تعرض خارطة الجاهزية، و`/api/health` تعرض صحة الخدمة فقط.

## قاعدة البيانات

راجع `drizzle/schema.ts` قبل أي تعديل، ولّد migration عبر `pnpm drizzle-kit generate`، راجع SQL الناتج، ثم طبّقه في بيئة آمنة وفق آلية المنصة. لا تنفذ أوامر حذف أو إعادة تهيئة على قاعدة إنتاجية قبل نسخة احتياطية واختبار استعادة.

## طريقة التكملة

عند إضافة قطاع أو وحدة أو عملية، حدّث القاموس والأنواع أولاً، ثم أضف إجراء tRPC محمياً، وربطه بـ`tenantAccess`، وسجل تدقيق مناسب، واختبارات لصلاحيات المستخدم وعزل المستأجر، ثم أضف واجهة إدخال حقيقية مع حالات loading/error/empty. لا تعرض نجاحاً ما لم تؤكده قاعدة البيانات أو مزود خارجي حقيقي.

الدفع والإشعارات والخدمات الخارجية يجب أن تدخل عبر adapters منفصلة بواجهات واضحة، مع secrets في مدير البيئة، والتحقق من التوقيع، وidempotency، وإعادة المحاولة الآمنة. لا تُفعّل التسوية بمجرد نجاح التحقق من webhook؛ الحالة الحالية مقصودة وهي `verified-pending`.

## النقل إلى تطبيق Native

المسار المعتمد هو إنشاء مشروع Expo SDK 54 مستقل، ثم نقل الأنواع المشتركة وعقود tRPC وقاموس القطاعات، مع استخدام `ScreenContainer` لكل شاشة و`FlatList` للقوائم وExpo Router للملاحة. يجب إنشاء اختبارات Android/iOS، إعداد package identifiers، إدارة deep links وOAuth، ثم بناء APK/IPA موقّع. لا يكفي تحويل امتداد PWA إلى APK لإثبات الجاهزية الأصلية.

## فحص قبل التسليم لجهة أخرى

نفّذ `pnpm check`, `pnpm test`, `pnpm test:e2e`, و`pnpm build`. راجع `SECURITY_AUDIT.md`، افحص `git diff --check`، وتأكد من عدم وجود أسرار عبر `find . -name '.env*'` و`git grep` على أسماء المفاتيح. سلّم ملف ZIP المصدر، لا مجلد `node_modules` ولا ملفات البناء الناتجة.

## المتغيرات المطلوبة خارج الحزمة

يجب إدخال `DATABASE_URL`, `JWT_SECRET`, `VITE_APP_ID`, `OAUTH_SERVER_URL`, `VITE_OAUTH_PORTAL_URL`, `BUILT_IN_FORGE_API_URL`, `BUILT_IN_FORGE_API_KEY`, و`PAYMENT_WEBHOOK_SECRET` عبر مدير أسرار البيئة في المنصة الجديدة، مع قيم مختلفة للتطوير والاختبار والإنتاج. لا تُنسخ القيم من جلسة أو جهاز إلى مستودع عام.
