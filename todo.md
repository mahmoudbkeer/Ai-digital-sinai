# تشغيل القطاعات والوحدات داخل AI DIGITAL SINAI

اكتملت إضافة نموذج تشغيل قطاعي داخل App Mode/PWA. كل قطاع يفتح وحداته، وكل وحدة تفتح عملياتها، وكل عملية تعرض مركز أمر سياقياً وتستدعي عقد الخادم للتحقق من المدخلات قبل اشتراط المصادقة ومساحة العمل.

- [x] تعريف نموذج موحد للقطاعات والفئات والتصنيفات والوحدات والعمليات.
- [x] إضافة أربعة عشر قطاعاً محلياً داخل `operationsCatalog.ts`.
- [x] إضافة دليل وحدات داخلي قابل للنقر والتنقل داخل App Mode.
- [x] بناء شاشة عمليات متغيرة حسب القطاع والتصنيف مع حالات جاهز/يتطلب إعداداً/مؤجل.
- [x] إضافة `POST /api/commands/prepare` للتحقق من القطاع والوحدة والعملية قبل أي تنفيذ.
- [x] ربط مركز الأوامر برسالة خادمية واشتراط المصادقة بدلاً من الاكتفاء برسالة محلية.
- [x] ربط خارطة التنفيذ المهنية من `/api/app-data` وعرضها داخل التطبيق.
- [x] مراجعة الموديولات والـserver وحماية HTTP ومؤشر observability وعقد webhook HMAC.
- [x] إضافة اختبارات النقر والتنقل والأوامر عبر Playwright على الهاتف.
- [x] تشغيل `pnpm check` و`pnpm test` و`pnpm test:e2e` و`pnpm build` بنجاح.
- [x] توثيق النموذج في `OPERATIONS_MODEL.md` وإجراء معاينة هاتف وحفظ checkpoint.

## حدود واضحة

التحقق الخادمي الحالي يثبت استقبال الأمر وصحة معرفاته واشتراط المصادقة؛ لا يسجل معاملة إنتاجية قبل ربط كل عملية بمسار API محمي وtenant context وسجل تدقيق وقاعدة بيانات. الأوامر المالية والصحية واللوجستية تحتاج موصلات ومراجعة أمنية قبل تفعيل التنفيذ الفعلي.

## مراجعة أمنية وحزمة النقل

- [x] فحص الأسرار والبيئة والاعتماديات ومسارات API والـCORS وHTTP headers.
- [x] مراجعة المدخلات وHMAC والأوامر والصلاحيات وعدم كشف البيانات أو المعلومات الحساسة.
- [x] إضافة تحسينات أمنية قابلة للإثبات واختبارات regression لها.
- [x] تحديث SECURITY وTRANSFER وREADME بتعليمات النقل والتشغيل دون أسرار.
- [x] تشغيل quality gates ثم إنشاء ZIP نظيف قابل للتعديل والنقل.
- [x] إصلاح تشغيل التطوير بحيث تمرر Vite مسارات `/api` إلى خادم Express وتظهر حالات الاتصال الحقيقية.
- [x] تحديث README.md بمرجع الأمان والنقل والتشغيل.
- [x] إنشاء ZIP مصدر نظيف بعد استبعاد الأسرار والاعتماديات والبناء والسجلات المؤقتة.
- [x] رفع المصدر النظيف إلى GitHub بعد التحقق من المصادقة ووجهة المستودع.

## مقترحات احترافية قابلة للتنفيذ

- [x] إضافة GitHub Actions لتشغيل check وtest وbuild وE2E.
- [x] إضافة توثيق حوكمة الفرع وقواعد المراجعة دون فرض إعدادات غير مؤكدة على المستودع.
- [x] تثبيت حد تشغيل أولي محمي يرفض التنفيذ قبل المصادقة ومساحة العمل، مع توثيق أن سجل التدقيق وكتابة قاعدة البيانات مؤجلان حتى ربط backend الإنتاجي.
- [x] تثبيت اختبارات رفض الأوامر غير الصحيحة كحد عزل أولي، مع إبقاء اختبار tenant isolation الكامل شرطاً لمرحلة ربط قاعدة البيانات.
- [x] مزامنة التحديثات مع GitHub والتحقق من CI والملفات الحساسة.
- [x] إضافة تحقق خادمي فعلي في `/api/commands/prepare` يعيد 401 عند غياب سياق المصادقة و403 عند فشل توقيع سياق المستخدم ومساحة العمل.
- [x] إضافة اختبار يثبت رفض الأمر عند غياب المصادقة أو مساحة العمل، ورفض تبديل المستخدم أو مساحة العمل مع التوقيع نفسه.
- [x] إضافة اختبار تكامل فعلي لمسار `/api/commands/prepare` يثبت 401 و403 من خادم Express.
- [x] تشخيص فشل Quality Gate في GitHub Actions وإصلاحه ثم إعادة تشغيله بنجاح.

## دفعة تحسينات مهنية مستعادة

- [x] إضافة CSP وHSTS وrequest ID وتسجيل أخطاء منظم دون كشف الترويسات الحساسة.
- [x] إضافة اختبارات تكامل لترويسات الأمان وسجل الأخطاء الفعلي.
- [x] تحديث README بشرح BASE_URL وسلوك test:smoke ومتطلبات البيئة.
- [x] تشغيل الفحوص ومزامنة آخر التغييرات مع GitHub والتحقق من CI.

## حزمة قيادة تقنية متقدمة

- [x] إضافة عقد correlation/request IDs موحدة مع ردود API ورسائل السجل.
- [x] إضافة سياسة idempotency للأوامر الحساسة قبل أي تنفيذ إنتاجي.
- [x] إضافة فحص readiness منفصل عن liveness يكشف الاعتماديات غير المربوطة بصدق.
- [x] إضافة وثيقة ADR/قرار معماري للحدود بين App Mode وbackend وقاعدة البيانات وExpo.
- [x] إضافة مصفوفة صلاحيات وأدوار متعددة مع توثيق ما هو منفذ وما هو مؤجل.
- [x] إضافة اختبارات regression للـwebhook وsecurity headers وIdempotency.
- [x] تشغيل الفحوص ومزامنة الحزمة مع GitHub والتحقق من Quality Gate.

## مرحلة واجهة الجاهزية والتشغيل

- [x] إضافة لوحة جاهزية داخل App Mode تعتمد على `/api/health` و`/api/readiness` و`/api/observability` الحقيقية.
- [x] تحسين مركز الأمر ليطلب Idempotency-Key ويعرض حالات 401 و403 و428 و409 و503 بوضوح عربي.
- [x] إضافة اختبارات E2E للتنقل ولوحة الجاهزية ومركز الأمر دون بيانات وهمية.
- [x] تحديث التوثيق بمسار الفحص اليدوي وروابط المعاينة.
- [ ] تشغيل الفحوص ومزامنة GitHub وحفظ checkpoint نهائي.
- [x] جعل اختبار لوحة الجاهزية مستقلاً عن وجود سر الدفع في بيئة التشغيل، مع قبول حالتي جاهز أو يحتاج إعداداً دون بيانات وهمية.
- [x] تحديث توقع E2E لرسالة مركز الأمر الجديدة بعد إضافة Idempotency والحماية متعددة الحالات.
- [x] توثيق الفحص اليدوي للوحة الجاهزية ومركز الأوامر داخل `/app` وربطه بعقود API الفعلية.
- [x] توثيق مسار المعاينة وحالات 401 و403 و428 و409 و503 داخل APP_MODE أو README.

## المرحلة Native المبنية على آخر نسخة GitHub

- [x] مزامنة وفحص آخر commit في GitHub قبل الدمج؛ تم التحقق من commit `c7b6618`.
- [x] دمج مشروع Native فعلي داخل المستودع من آخر نسخة GitHub، يتضمن Android وiOS؛ مسار Expo الكامل يبقى مرحلة لاحقة منفصلة.
- [x] تحديد قيم Android SDK الفعلية: `compileSdk=35` و`targetSdk=35` و`minSdk=26`، مع CI يستخدم `android-actions/setup-android@v3`.
- [x] بناء APK debug جديد والتحقق من وجوده وحساب SHA-256؛ الهاش موثق في `ANDROID_BUILD_STATUS.md`.
- [ ] التحقق من نجاح Android CI وتوثيق رابط التشغيل وartifact.
- [ ] مزامنة نتيجة البناء والوثائق مع GitHub وحفظ checkpoint.
- [x] إصلاح اعتماد PostgreSQL/TypeScript الذي ظهر بعد دمج آخر نسخة GitHub قبل بناء Native، دون تعطيل مسار SQLite أو الويب.
- [x] إصلاح مشغل التطوير بعد دمج آخر نسخة GitHub بحيث يختار قاعدة محلية صريحة ولا يتوقف بسبب DATABASE_URL الإنتاجي، مع إبقاء الإنتاج على PostgreSQL.
- [x] عزل اختبار command endpoint عن DATABASE_URL الإنتاجي باستخدام SQLite اختبارية صريحة حتى لا يتعطل قبل Gradle.
- [x] توثيق قيم Android ونتيجة بناء APK وSHA-256 وحالة CI في `ANDROID_BUILD_STATUS.md` دون رفع APK أو `local.properties` إلى المستودع.
